<p id="notice"><%= notice %></p>

<h1>Decisions</h1>

<table>
  <thead>
    <tr>
      <th>Decision title</th>
      <th>Decision desc</th>
      <th>Decision position</th>
      <th>Decision source url</th>
      <th>Decision date</th>
      <th>Judge</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @decisions.each do |decision| %>
      <tr>
        <td><%= decision.decision_title %></td>
        <td><%= decision.decision_desc %></td>
        <td><%= decision.decision_position %></td>
        <td><%= decision.decision_source_url %></td>
        <td><%= decision.decision_date %></td>
        <td><%= decision.judge %></td>
        <td><%= link_to 'Show', decision %></td>
        <td><%= link_to 'Edit', edit_decision_path(decision) %></td>
        <td><%= link_to 'Destroy', decision, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Decision', new_decision_path %>
