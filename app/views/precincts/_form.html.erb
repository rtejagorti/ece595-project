<%= form_for(precinct) do |f| %>
  <% if precinct.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(precinct.errors.count, "error") %> prohibited this precinct from being saved:</h2>

      <ul>
      <% precinct.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :precinct_num %>
    <%= f.text_field :precinct_num %>
  </div>

  <div class="field">
    <%= f.label :precinct_type %>
    <%= f.text_field :precinct_type %>
  </div>

  <div class="field">
    <%= f.label :city_id %>
    <%= f.text_field :city_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
