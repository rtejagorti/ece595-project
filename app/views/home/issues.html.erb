<h1>Issues</h1>
<p>Select an issue&hellip;</p>
<table class="detail-table issue-table">
    <thead>
        <tr>
            <th>Title</th>
            <th>Description</th>
            <th>Type</th>
            <th>Details</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>{{referenda.title}}</td>
            <td>{{referenda.desc}}</td>
            <td>{{referenda.type}}</td>
            <td><a href="{{this_will_link_to_an_issue}}">More&hellip;</a></td>
        </tr>
        <tr>
            <td>{{referenda.title}}</td>
            <td>{{referenda.desc}}</td>
            <td>{{referenda.type}}</td>
            <td><a href="{{this_will_link_to_an_issue}}">More&hellip;</a></td>
        </tr>
    </tbody>
</table>
